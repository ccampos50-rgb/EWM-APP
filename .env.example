# Copy to .env and fill in. Do not commit .env.
# Shared across apps where useful; app-specific vars live in apps/*/.env.example

# Supabase (new project — not shared with Tom)
SUPABASE_URL=
SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=
