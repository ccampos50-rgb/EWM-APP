/**
 * Push notifications scaffolding (currently disabled).
 *
 * To re-enable:
 *   1. `pnpm add expo-notifications expo-device` in apps/mobile
 *   2. Set up Apple Push Notifications service key (eas credentials → Push Notifications)
 *   3. Restore the original implementation from git history
 *   4. Rebuild
 */
export async function registerForPushNotifications(): Promise<string | null> {
  return null;
}
